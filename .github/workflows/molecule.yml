# SPDX-FileCopyrightText: 2026 Slavi Pantaleev
# SPDX-FileCopyrightText: 2026 spatterlight
#
# SPDX-License-Identifier: AGPL-3.0-or-later

---
name: molecule

on:  # yamllint disable-line rule:truthy
  push:
    paths-ignore:
      - "docs/**"
      - "README.md"
      - "CHANGELOG.md"
      - "LICENSE"
      - "LICENSES/**"
      - "REUSE.toml"
  pull_request:
    paths-ignore:
      - "docs/**"
      - "README.md"
      - "CHANGELOG.md"
      - "LICENSE"
      - "LICENSES/**"
      - "REUSE.toml"
  workflow_dispatch:

jobs:
  molecule:
    name: Run Molecule scenario '${{ matrix.scenario }}' on ${{ matrix.distro }}
    runs-on: ubuntu-latest

    # Same-repository pull requests already run via the push event;
    # run pull_request jobs only for pull requests from forks.
    if: >-
      github.event_name != 'pull_request'
      || github.event.pull_request.head.repo.full_name != github.repository

    strategy:
      # Both distributions are covered, and so is each of the three database
      # backends, without running the full nine-job product on a shared runner
      # pool. The scenarios differ in what they check, not in how they install.
      matrix:
        include:
          - distro: ubuntu2604
            scenario: default
          - distro: ubuntu2604
            scenario: postgres
      fail-fast: false

    env:
      MOLECULE_DISTRO: ${{ matrix.distro }}
      PY_COLORS: "1"
      ANSIBLE_FORCE_COLOR: "1"

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7.0.0
        with:
          path: ihatemoney

      - name: Set up Python
        uses: actions/setup-python@v6.3.0
        with:
          python-version-file: ihatemoney/.python-version
          cache: pip
          cache-dependency-path: ihatemoney/molecule/requirements.txt

      - name: Install Python dependencies
        working-directory: ihatemoney
        run: pip install -r molecule/requirements.txt

      - name: Cache Ansible collections
        uses: actions/cache@v6.1.0
        with:
          path: ~/.ansible/collections
          key: ansible-collections-${{ hashFiles('ihatemoney/molecule/*/requirements.yml') }}

      - name: Run Molecule
        working-directory: ihatemoney
        run: molecule test --scenario-name ${{ matrix.scenario }}
