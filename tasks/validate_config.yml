# SPDX-FileCopyrightText: 2023 Julian-Samuel Gebühr
# SPDX-FileCopyrightText: 2023 MASH project contributors
# SPDX-FileCopyrightText: 2023-2025 Slavi Pantaleev
# SPDX-FileCopyrightText: 2025 IUCCA
# SPDX-FileCopyrightText: 2025, 2026 Suguru Hirahara
#
# SPDX-License-Identifier: AGPL-3.0-or-later

---
- name: (Deprecation) Catch and report renamed settings
  ansible.builtin.fail:
    msg: >-
      Your configuration contains a variable, which now has a different name.
      Please change your configuration to rename the variable (`{{ item.old }}` -> `{{ item.new }}`).
  when: "lookup('ansible.builtin.varnames', ('^' + item.old + '$'), wantlist=True) | length > 0"
  with_items:
    - { "old": "ihatemoney_database_hostname", "new": "<Replace with ihatemoney_database_mysql_hostname or ihatemoney_database_postgres_hostname>" }
    - { "old": "ihatemoney_database_port", "new": "<Replace with ihatemoney_database_mysql_port or ihatemoney_database_postgres_port>" }
    - { "old": "ihatemoney_database_username", "new": "<Replace with ihatemoney_database_mysql_username or ihatemoney_database_postgres_username>" }
    - { "old": "ihatemoney_database_password", "new": "<Replace with ihatemoney_database_mysql_password or ihatemoney_database_postgres_password>" }
    - { "old": "ihatemoney_database_sslmode", "new": "ihatemoney_database_postgres_sslmode" }
    - { "old": "ihatemoney_environment_variable_ihatemoney_port", "new": "ihatemoney_container_http_port" }
    - { "old": "ihatemoney_http_header_content_permission_policy", "new": "ihatemoney_http_header_permissions_policy" }
    - { "old": "ihatemoney_container_image_force_pull", "new": "<removed> (the new community.docker.docker_image_pull module handles this natively)" }

- name: Fail if required I hate money settings not defined
  ansible.builtin.fail:
    msg: >-
      You need to define a required configuration setting (`{{ item }}`).
  when: "lookup('vars', item, default='') | string | length == 0"
  with_items:
    - ihatemoney_uid
    - ihatemoney_gid
    - ihatemoney_hostname
    - ihatemoney_path_prefix
    - ihatemoney_container_network
    - ihatemoney_environment_variable_secret

- name: Fail if I hate money database type is invalid
  ansible.builtin.fail:
    msg: >-
      ihatemoney_database_type must be 'mysql', 'postgres', or 'sqlite'.
  when: "ihatemoney_database_type not in ['mysql', 'postgres', 'sqlite']"

# The container's entrypoint interpolates these into a Python configuration
# file without quoting them (`MAIL_PORT = $MAIL_PORT`), so an empty value there
# is a syntax error that only shows up as I hate money failing to boot.
- name: Fail if I hate money mailer settings are incomplete
  ansible.builtin.fail:
    msg: >-
      You need to define a required configuration setting (`{{ item }}`), because `ihatemoney_email_host` is set.
  when: "ihatemoney_email_host | string | length > 0 and lookup('vars', item, default='') | string | length == 0"
  with_items:
    - ihatemoney_email_port
    - ihatemoney_use_tls
    - ihatemoney_use_ssl

- name: Run if Traefik is enabled
  when: ihatemoney_container_labels_traefik_enabled | bool
  block:
    - name: Fail if Traefik settings required for I hate money are not defined
      ansible.builtin.fail:
        msg: >-
          You need to define a required configuration setting (`{{ item.name }}`).
      when: "item.when | bool and lookup('vars', item.name, default='') | string | length == 0"
      with_items:
        - { "name": "ihatemoney_container_labels_traefik_hostname", when: true }
        - { "name": "ihatemoney_container_labels_traefik_path_prefix", when: true }
        - { "name": "ihatemoney_container_labels_traefik_middleware_basic_auth_users", when: "{{ ihatemoney_container_labels_traefik_middleware_basic_auth_enabled }}" }

    # We ensure it doesn't end with a slash, because we handle both (slash and no-slash).
    # Knowing that `ihatemoney_container_labels_traefik_path_prefix` does not end with a slash
    # ensures we know how to set these routes up without having to do "does it end with a slash" checks elsewhere.
    - name: Fail if ihatemoney_container_labels_traefik_path_prefix ends with a slash
      ansible.builtin.fail:
        msg: >-
          ihatemoney_container_labels_traefik_path_prefix (`{{ ihatemoney_container_labels_traefik_path_prefix }}`) must either be `/` or not end with a slash (e.g. `/ihatemoney`).
      when: "ihatemoney_container_labels_traefik_path_prefix != '/' and ihatemoney_container_labels_traefik_path_prefix[-1] == '/'"
