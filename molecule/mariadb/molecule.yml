# SPDX-FileCopyrightText: 2026 Suguru Hirahara
# SPDX-FileCopyrightText: 2026 spatterlight
#
# SPDX-License-Identifier: AGPL-3.0-or-later

---
dependency:
  name: galaxy
  options:
    requirements-file: requirements.yml
    force: true
driver:
  name: docker
platforms:
  - name: ihatemoney-${MOLECULE_DISTRO:-ubuntu2604}-mariadb
    image: "geerlingguy/docker-${MOLECULE_DISTRO:-ubuntu2604}-ansible:latest"
    command: ${MOLECULE_DOCKER_COMMAND:-""}
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:rw
    cgroupns_mode: host
    privileged: true
    pre_build_image: true
provisioner:
  name: ansible
  config_options:
    defaults:
      callback_result_format: yaml
  inventory:
    group_vars:
      all:
        # Helper variables
        ihatemoney_debug_service_enabled: false

        # Variables for I hate money
        ihatemoney_uid: 1000
        ihatemoney_gid: 1000
        ihatemoney_scheme: http
        ihatemoney_hostname: localhost
        ihatemoney_container_labels_traefik_enabled: false
        ihatemoney_container_http_host_bind_port: 8000
        ihatemoney_environment_variable_secret: secret
        ihatemoney_database_type: mysql

        # Variables required for I hate money to connect to the MariaDB database (via Unix socket)
        ihatemoney_database_mysql_username: my_database_username
        ihatemoney_database_mysql_password: my_database_password
        ihatemoney_database_name: ihatemoney
        ihatemoney_database_mysql_socket_enabled: true
        ihatemoney_database_mysql_socket_path_host: "{{ mariadb_run_path }}"
        ihatemoney_systemd_required_services_list_auto: |
          {{
            (['mariadb.service'])
          }}

        # Variables for MariaDB
        mariadb_uid: 1000
        mariadb_gid: 1000
        mariadb_root_password: mariadb_password
        mariadb_container_unix_socket_enabled: true
        mariadb_managed_databases: |
          {{
            [{
              'name': ihatemoney_database_name,
              'username': ihatemoney_database_mysql_username,
              'password': ihatemoney_database_mysql_password,
            }]
          }}
  env:
    # Workaround for https://github.com/ansible/molecule/issues/4391
    ANSIBLE_ROLES_PATH: ${MOLECULE_PROJECT_DIRECTORY}/..:/.ansible/roles:/usr/share/ansible/roles:/etc/ansible/roles:~/.ansible/roles
scenario:
  # See: https://docs.ansible.com/projects/molecule/configuration/?h=test_sequence#scenario
  test_sequence:
    - dependency
    - cleanup
    - destroy
    - syntax
    - create
    - prepare
    - converge
    # Disable idempotence test for now due to ansible-role-mariadb
    # - idempotence
    - side_effect
    - verify
    - cleanup
    - destroy
verifier:
  name: ansible
